@name BUBBLES
@inputs 
@outputs 
@persist 
@trigger 
runOnTick(1)
runOnChat(1)
if(first()){
 holoCreate(1, entity():pos())   
 function void instructions() {
 hint("To USE: .f <Player> this puts them in a lovely bubble, To pop said bubble  : type stop in chat. To see this again type .help in chat! have fun",25)
}
instructions()
}
O = owner()
if(chatClk(O)&O:lastSaid()==".help"){ instructions() }

S = O:lastSaid():explode(" ")
E = entity()
Ran = random(-255)
if(S:string(1) == ".f"){
    hideChat(1)
   
 if(chatClk(O)){ print("{Corona's Bubble's}"+" "+ O:lastSaid()+ " " +"[HIDDEN]") holoCreate(1) }
    Find = findPlayerByName(S:string(2))
    if(Find:isPlayer()){ Player = Find }
        if(Player:isPlayer()){
         holoPos(1, Player:pos() + vec(0,0,50))
         holoModel(1, "sphere")
         holoScale(1, vec(-15,-15,-15))
         holoParent(1, Player)
         holoColor(1, vec(Ran,Ran,Ran))
        for(I=1,10){ #remove the for loop in game, afraid to without editor
        
            }
        }
}
if(chatClk(O)&S:string(1)=="stop"){ holoPos(1,E:pos()) holoModel(1,"") soundPurge() hideChat(1) print("{Corona's Blinder}"+" "+ O:lastSaid()+ " " +"[HIDDEN]") holoDeleteAll()}
if(holoEntity(1) == noentity()){ reset() }