@name Manual-Turret 2
@persist Vec:vector C
@outputs Fire 
@inputs 
runOnTick(1)
#Variables
    E = entity()
    P = owner()
    C = 0
    Ran = random(255,0),random(255,0),random(255)
    EW = E:isConstrainedTo()
#Instructions / moving prop / shooting
if(first()){
    printColor(vec(random(255,0),random(255,0),random(255)),"Place this chip on a unfrozen prop.")
        printColor(vec(random(255,0),random(255,0),random(255)),"When ready type !ready in chat!(When first placed the prop faces you!)")
        printColor(vec(random(255,0),random(255,0),random(255)),"Also copy the SetMass E2 at the very bottom of the chip and in a new e2 workspace paste the code and spawn them on the turrent(s)!)")
        printColor(vec(random(255,0),random(255,0),random(255)),"Only if you want a more stable following turret.")        
        EW:setAng((P:pos()+vec(0,0,60)-EW:pos()):toAngle())
        E:setAlpha(0)
        E:setMass(0)
}
    
    if(P:keyAttack2()){
    Fire = 1
    C = 1    

}else{
Fire = 0
}

    if(C){
EW:setAng(P:eyeAngles())
}

#Follows you
Vec = owner():pos()-(entity():pos()-vec(0,0,100))
EW:applyOffsetForce($Vec*50+Vec*EW:mass(),Vec) 
#SetMass E2
#[
@name SetMass E2
runOnTick(1)
EW = entity():isConstrainedTo()
if(first()){
    EW:setMass(0)
    timer("delete",1)  
}
if(clk("delete")){
    selfDestruct()
}
]#