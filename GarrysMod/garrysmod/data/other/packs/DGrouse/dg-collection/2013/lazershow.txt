@name Disco
@inputs GPS:vector
@outputs Pos:vector FadeTime:number Size:number X:number

if(first()){
    FadeTime=1
    Size=20
    X=1
}

runOnTick(5000)
if(ops()<40){
    Pos = vec(random(-250,250)+GPS:x(),random(-200,200)+GPS:y(),random(-200,200)+GPS:z())
}
