@name 
@inputs 
@outputs 
@persist Ent:entity
@trigger all
runOnTick(1)
if (owner():aimEntity():pos() != vec(0,0,0)) {Ent = owner():aimEntity()}

if (owner():keyAttack2()) {
Ent:applyForce((-Ent:vel() + vec(0,0,80)) * Ent:mass())
Ent:applyAngForce((-Ent:angVel() + ang(curtime()*10,curtime()*10,0))*Ent:mass())
}
