@name 
@inputs 
@outputs 
@persist Prop:entity Player:entity
@trigger all
findByModel("models/props_junk/sawblade001a.mdl")
Prop = find()

findByClass("players")
findExcludePlayer("Whosdr")
Player = find()

interval(20)
Prop:applyAngForce(-Prop:angles() + -Prop:angVel() + ang(0,curtime()*10,0))

Prop:applyForce((-Prop:pos() + -Prop:vel() + Player:pos() + vec(0,0,100))*Prop:mass())
