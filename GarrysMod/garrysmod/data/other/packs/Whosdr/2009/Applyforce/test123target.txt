@name 
@inputs A B
@outputs 
@persist Target:vector Prop:entity
@trigger all
if (first()) {
Target = owner():pos() + vec(0,0,100)
}

interval(20)

if (A) {
findByModel("models/props_junk/wood_crate001a.mdl")
Prop = find()
} elseif (B) {
findByModel("models/props_junk/wood_crate002a.mdl")
Prop = find()
} else {
findByModel("loly")
}


if (A | B) {
Prop:applyForce((-Prop:pos() + -Prop:vel() + Target)*Prop:mass())
}

