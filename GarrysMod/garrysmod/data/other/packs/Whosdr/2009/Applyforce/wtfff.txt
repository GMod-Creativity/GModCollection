@name 
@inputs A
@outputs 
@persist Pos:vector Ent:entity
@trigger all
interval(20)
Pos = entity():pos()
if (A) {
findByModel("models/props_c17/oildrum001.mdl")
Ent = find()
} else {
findByModel("models/error.mdl")
}


if (A) {
Ent:applyForce((-Ent:pos() + Ent:vel() + Pos)*Ent:mass())
}
