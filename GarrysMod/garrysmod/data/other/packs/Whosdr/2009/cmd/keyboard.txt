@name Interface Device
@inputs Key Use
@outputs Keys:string
@persist 
@trigger 

if (Key == 13) {Keys = ""} 
elseif(Key == 127) {Keys = Keys:sub(1,Keys:length()-1)} elseif (Key == 154 | Key == 175) {}
else {Keys += toChar(Key)}

if (!Use) {Keys = ""}
