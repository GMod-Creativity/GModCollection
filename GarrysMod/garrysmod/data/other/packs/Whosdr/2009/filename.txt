@name 
@inputs Screen:wirelink
@outputs X Y
@persist X Y BX BY DX DY XX YY
@trigger 

if (first()) {
X = 64
Y = 64
DX = randint(10)
DY = randint(5)
SizeX = 1
SizeY = 1
BX = 128 - (SizeX) - 1
BY = 128 - (SizeY) - 1                                     
Screen[1048574] = 1 #reset
}

interval(10)

if (X >= BX | X <= 1) {DX = -DX}
if (Y >= BY | Y <= 1) {DY = -DY}
X += DX
Y += DY

#for (XX = 1, SizeX) {
#for (YY = 1, SizeY) {
#Screen[XX+(YY*128)] = 999999
#}
#}

Screen[X+(Y*128)] = random(0,999999)
