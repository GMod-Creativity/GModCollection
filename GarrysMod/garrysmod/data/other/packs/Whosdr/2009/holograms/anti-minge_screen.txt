@name 
@inputs Ent:entity A
@outputs 
@persist 
@trigger all
if (first()) {
holoCreate(1)
}
interval(20)
holoPos(1,Ent:pos() + (Ent:eye()*150) + vec(0,0,60))
holoScale(1,vec(0,100*A,100*A))
holoAng(1,Ent:angles())
holoColor(1,vec(0,0,0))
