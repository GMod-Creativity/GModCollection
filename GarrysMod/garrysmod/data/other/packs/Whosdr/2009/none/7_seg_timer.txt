@name 
@inputs On Set Reset
@outputs Minute Second Seconds
@persist X
@trigger all
interval(1000)
if (first() | Reset) {X = Set}
if (On & X > 0) {X -= 1}
Minute = floor(X/60)
Second = floor((X - (Minute*60))/10)
Seconds = floor(X - (Minute*60)) - (Second*10)
