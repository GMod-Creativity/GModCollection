@name 
@inputs 
@outputs Pos:vector
@persist Players:array X Find
@trigger 
interval(20)
if (first()) {
findByClass("player")
findExcludePlayer(owner())
Players = findToArray()
}
if (!Find) {
if (X < Players:count()) {X += 1} else {X = 0}
}

if (Players:entity(X):weapon():model() == "models/weapons/v_pistol.mdl") {
Find = 1
Pos = Players:entity(X):pos()
print(Players:entity(X):name())
} else {
Find = 0
}
