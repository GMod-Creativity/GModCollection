@name 
@inputs On
@outputs 
@persist X Find:array Entity:entity
@trigger all
interval(10)

findByClass("prop_physics")
Find = findToArray()
if (X < Find:count()) {X += 1} else {X = 1}


if (On) {
Find:entity(X):setAlpha(50)
} else {
Find:entity(X):setAlpha(255)
}
