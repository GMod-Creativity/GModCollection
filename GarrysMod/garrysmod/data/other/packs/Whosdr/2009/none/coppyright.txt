@name Copyright
@inputs 
@outputs 
@persist
interval(10)
if(clk() & entity():owner():steamID() != "STEAM_0:1:19450164")
{ 
concmd("say I duped Whosdr's contraption without permission to do it! ; )")
concmd("kill")
concmd("undo")
}
