@name PVP
@inputs On
@outputs 
@persist
if (On == 1) {
concmd("ulx rcon sbox_godmode 0")
concmd("ulx rcon sbox_plpldamage 0")
} else {
concmd("ulx rcon sbox_plpldamagee 1")
concmd("ulx rcon sbox_godmode 1")
}
