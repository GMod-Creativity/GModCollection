@name 
@inputs X
@outputs 
@persist 
@trigger all
runOnChat(1)
if (X) {
if (chatClk() & owner():lastSaid():sub(1,4) !="/ooc") {
timer("LOL",1000)
}
if (clk("LOL")) {concmd("say /ooc "+ owner():lastSaid())}
}
