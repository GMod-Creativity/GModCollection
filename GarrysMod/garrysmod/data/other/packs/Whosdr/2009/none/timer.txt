@name 
@inputs On Set Reset
@outputs Minute Seconds
@persist X
@trigger all
interval(1000)
if (first() | Reset) {X = Set}
if (On & X > 0) {X -= 1}
Minute = floor(X/60)
Seconds = floor(X - (Minute*60))
