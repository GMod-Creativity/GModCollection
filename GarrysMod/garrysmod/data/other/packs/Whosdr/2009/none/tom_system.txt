@name 
@inputs Next Fire
@outputs Pos:vector Name:string
@persist X Ent:array Name:string Pos:vector
@trigger all
interval(100)
findByClass("player")
#findExcludePlayer("Whosdr")
#findExcludePlayer("Cliph")
Ent = findToArray()
if (Next & X <= Ent:count()) {X += 1}
elseif (Next & ~Next & X >= Ent:count()) {X = 1}
Pos = Ent:entity(X):pos()
Name = Ent:entity(X):name()
