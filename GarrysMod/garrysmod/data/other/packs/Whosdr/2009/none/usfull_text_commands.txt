@name 
@inputs 
@outputs 
@persist X
@trigger all
runOnChat(1)
if (owner():lastSaid() == "!brb" & X == 0) {concmd("ulx csay Be right back guys.") X = 1}
if (owner():lastSaid() == "!back" & X == 1) {concmd("ulx csay Im back!") X = 0}
