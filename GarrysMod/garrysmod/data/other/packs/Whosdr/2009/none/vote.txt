@name 
@inputs 
@outputs String:string
@persist Players:table Values:string X Ent:array
@trigger 
runOnChat(1)

if (lastSpoke():lastSaid():explode(" "):string(1) == "/vote") {
    
if (!Players[lastSpoke():name(),string] & lastSaid():explode(" "):string(2):toNumber() < 3) {
Players[lastSpoke():name(),string] = lastSaid():explode(" "):string(2) + ","
X = 0
}
}
String = ""
findByClass("player")
Ent = findToArray()
for (X = 0,Ent:count()) {
String += Players[Ent[X,entity]:name(),string]
}
if (X == Ent:count()) {
print(String)
}
