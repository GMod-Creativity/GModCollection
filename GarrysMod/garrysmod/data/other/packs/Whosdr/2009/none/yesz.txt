@name 
@inputs 
@outputs 
@persist 
@trigger all
interval(1000)
findInSphere(owner():pos(),1000)
findByClass("player")
findExcludePlayer(owner())
if (find() != owner()) {
concmd("ulx slay " + find():name())
} else {
}
