@name 
@inputs 
@outputs 
@persist X Y Ent:entity Ents:array Limit
@trigger 
interval(200)
if (first()) {Y = 1 Limit = 100}
if (owner():keyAttack1()) {concmd("say HAAAAAAX!")}

if (owner():keyAttack2()) {
if (Y <= Limit) {
Ent = propSpawn("models/props_lab/monitor01a.mdl",1)
Y++
}
Ent:setMass(50000)
Ents[Y,entity] = Ent
Ent:setPos(owner():pos() + vec(0,0,75) + owner():forward()*30)
Ent:setAng(ang(90,0,90) + owner():angles())
Ent:propFreeze(0)
Ent:applyForce(owner():eyeAngles():forward()*500000000000000*Ent:mass())
Ent:applyAngForce(-Ent:angles() + ang(0,curtime()*10000000,0)*Ent:mass())
#Ent:parentTo(owner())
}


if (owner():keyUse()) {
Ents:propDelete()
Y = 1
}
