@name 
@inputs 
@outputs 
@persist Explode:array
@trigger 
runOnChat(1)

if (chatClk(owner())) {

LS = owner():lastSaid():explode(" ")

if (LS[1,string] == "!file") {

if (LS[2,string]:find(".txt")) {

fileWrite("CC/"+LS[2,string],LS[3,string])

Explode = LS[2,string]:explode("/")

fileAppend("CC/"+LS[2,string]:replace(Explode:string(Explode:count()),"") + "index.txt",Explode:string(Explode:count()))


} elseif (LS[2,string]:index(LS[2,string]:length()) == "/" & !fileRead("/CC/"+LS[2,string]+"index.txt")) {

fileWrite("/CC/"+LS[2,string]+"index.txt","[]")
}
}
}
