@name AllTalk client
runOnChat(1)
if(chatClk()){
if(lastSpoke() != owner()){
    hideChat(1)
    Team = lastSpoke():team()
    if(lastSpoke():isAdmin()){printColor(vec(255,0,0),lastSpoke():name(),": ",vec(255,255,0),lastSaid())}
    elseif(lastSpoke():isSuperAdmin()){printColor(vec(255,0,0),lastSpoke():name(),": ",vec(255,255,255),lastSaid())}
    else{printColor(teamColor(Team),"[",Team:teamName(),"] ",vec(0,255,255),lastSpoke():name(),vec(255,255,255),": ",lastSaid())}
}}

